#ifndef __VER_H__
#define __VER_H__

// version variable that will be substituted by cmake
// This shows an example using the $ variable type
const char* ver = "${VERSION}";
const char* major_ver="${MAJOR_VERSION}";
const char* minor_ver="${MINOR_VERSION}";
const char* build_ver="${BUILD_NUMBER}";
const char* remainder_ver="${VERSION-REMAINDER}";


#endif